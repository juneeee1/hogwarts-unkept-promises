# 2026-09-07 改造验证

- TypeScript `npx tsc --noEmit`：通过。
- 场景几何测试 `npm test`：通过。7 个安全落点、全部地面场景连通、墙体/桌椅/桥边/天文台边界、餐具边缘余量；4 段楼梯两种对接方向的上下行，以及旋转过程中的载人支撑与回廊防坠落。
- 总场景约 146.7 万三角形，采用同材质合并、共顶点索引、火焰/烟雾实例化与自适应分辨率。
- `npm run build:storvia`、`npm run build`：通过。Three.js 懒加载包有 >500kB 构建提示，压缩传输约 175kB，无构建错误。
- 应用自有代码定向 oxlint：通过；仓库全量 lint 仍受未改动的组件库和旧 hook 中的语义/React 编译器规则错误影响，未改写无关库文件。
- 浏览器：检查桌面 1280×720、手机竖屏 390×844、手机横屏 844×390 的场景、地图、控制按钮与转向状态。页面无控制台 error。非手机 GPU/Storvia 真机结果，不能代表所有移动设备性能。
- 新增肖像和纹章使用内置 imagegen；PBR 图像压缩前 8,447,577 字节，压缩后 3,212,980 字节，降低约 62%。
- 既有线上 Sites 页面未自动发布；本目录包含本次实现和独立 Storvia ZIP。

## 独立成品包复核

- `dist-storvia` 在本地静态服务返回 HTTP 200，17 张本地 WebP 贴图完整，成品包页面无控制台 error。
- ZIP 为 4,215,958 字节（约 4.22 MB），22 个文件，入口 `index.html` 在根目录；ZIP CRC 与相对资源引用检查通过。
- 成品包实际键盘行走使楼梯上的玩家视点由 2.05 米升到 2.41 米，旋转交互能够携带玩家，单独的几何测试覆盖全部 4 段楼梯上下行。
- 浏览器检查不等同于 Storvia 内真实触摸屏、GPU、WebView 或平台上传验收。

- 成品复核修复：GTAO 深度/法线覆盖材质不识别透明 sprite，导致魔杖光晕产生黑色方块。现仅在 AO pass 隐藏透明光效与玻璃，再恢复正常渲染，保留真实火焰深度遮挡。

## v2：魔杖、手机界面与分院帽

- 只显示魔杖；已移除手部、袖口和全屏调用代码；页面无 kbd 元素。
- 分院帽的真实几何模型在独立检查页查看了正面、侧面；没有使用贴图代替三维模型。纹理为确定性程序材质，无额外图像网络请求。
- `npm test` 通过：1,494,872 三角形，1,534,692 顶点，83 碰撞体，21,514 个可达采样网格。没有提高原来的 150 万三角形预算。
- `npx tsc --noEmit` 与应用自有代码、模型检查页的定向 oxlint 通过。
- 浏览器实际 CSS 视口 390×844、844×390 验证：魔杖无手部、楼梯点击转向可用、按键徽标为 0、全屏入口不存在、设置可操作、无横向页面溢出，控制台无 error。另修复 260px 窄视口下设置画质选项拥挤。浏览器当前存在 150% 缩放，视口工具的尺寸按比例设置并通过 DOM 读取实际 CSS 尺寸，未改动用户缩放偏好。
- `build:storvia` 通过，ZIP CRC、根入口与相对脚本/样式路径通过；模型检查页未打入游戏。
- 仅本地预览和交付包，未发布到线上；未进行 Storvia 真机或计费测试。本轮未调用任何付费游戏生成接口。
- v2 ZIP：`hogwarts-nocturne-wand-hat-v2-storvia-20260907.zip`，4,216,949 字节，22 个文件。

## v3：圆凳红布修复

- 原因：布料波动会进入凳面，固定 z 方向的垂落线没有贴合圆凳，下缘可能穿入外张椅腿。
- 现在按每一列的真实圆凳边缘构造平铺、圆弧翻折和向外下垂三段曲面；凳面始终保留正间隙。帽子底部抬高余量，轻摆限制在 ±0.018 弧度。
- 礼堂与近景检查页统一使用 `buildSortingHatDisplay`，避免检查的是另一张凳子。
- 正面、侧面、背面视觉检查通过。新测试使用布料实际三角形边与实体显示模型的相交检测，覆盖静止及左右轻摆；全部既有场景测试也通过。
- 最终场景为 1,496,216 个三角形、83 个碰撞体；没有提高 150 万三角形预算。
- TypeScript、定向 oxlint 与静态构建通过；ZIP CRC、根入口和相对资源路径通过。
- 本轮没有实装新叙事、彩蛋或 AI，第二版讨论稿和原著依据另存；没有线上发布。
- v3 ZIP：`hogwarts-nocturne-cloth-v3-storvia-20260907.zip`，4,217,116 字节。
